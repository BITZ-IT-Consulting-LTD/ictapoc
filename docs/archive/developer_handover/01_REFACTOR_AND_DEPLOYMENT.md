# Code Audit & Refactor Plan: GOK Repeatable Services Platform

## Overview
I have conducted an audit of the current `ictapoc` codebase against the developer handover documents. The infrastructure and data models are roughly 80% aligned with the design blueprints, but there are critical gaps in the **Workflow Engine** logic and **Security (RBAC)** implementation.

## 1. Refactor Requirements

### **A. Backend (Django)**
- **Workflow Engine Logic:** The `models.py` has a `WorkflowStep` model, but the `workflow.py` logic needs to be fully implemented to handle automated transitions (e.g., from `api_call` steps) and manual approvals.
- **Audit Logging:** The `AuditLog` model exists, but we need to ensure that every state change in `ServiceRequest` automatically triggers a log entry.
- **SLA Monitoring:** The `ServiceConfig` includes an SLA field, but we need a background task (Celery) to monitor these and flag requests as `is_escalated`.

### **B. Frontend (Vue 3)**
- **Dynamic Form Generation:** The `FormSchemaBuilder.vue` needs to be verified against the `ServiceConfig.config` JSON structure to ensure it correctly renders fields like `full_name` or `dob`.
- **Dashboard Synchronization:** Ensure the `OfficerWorkbench` correctly filters pending requests based on the user's `role` (e.g., Registrar sees only registrar-specific steps).

## 2. Technical Modifications (In Progress)
I will now proceed with the following modifications:
1. **Piping State Changes:** Update `ServiceRequest.save()` or use signals to ensure AuditLogs are created.
2. **Workflow Progression:** Finalize the `advance_workflow` function to move a request to the next `sequence` in the `WorkflowStep` list.

## 3. Deployment Documentation

### **A. Local Setup (Development)**
1. **Environment Variables:** Create a `.env` file from the `.env.example`.
2. **Build Containers:** 
   ```bash
   docker-compose build
   ```
3. **Run Services:** 
   ```bash
   docker-compose up -d
   ```
4. **Initialize DB:**
   ```bash
   docker-compose exec backend python manage.py migrate
   docker-compose exec backend python manage.py loaddata mda_seed_data.json
   ```

### **B. Production Setup**
1. **Infrastructure:** Recommended AWS EC2/RDS or similar cloud environment.
2. **Reverse Proxy:** The included `nginx` container is configured to handle routing between the frontend (port 5173) and backend (port 8000).
3. **Security:** Update `SECRET_KEY`, `DEBUG=False`, and use a production-grade WSGI server like `gunicorn`.

---
*Generated by Loshie (AI Assistant)*
